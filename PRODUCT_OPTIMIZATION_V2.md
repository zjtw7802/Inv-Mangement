# BeautyGuard 产品优化方案（基于当前原型 V2）

## 0. 目标
在保留你现有原型视觉风格与核心结构（囤货 / 使用 / 空瓶 / 设置）的前提下，提升：
1. 新用户首周留存
2. 临期处理效率
3. 付费转化（高级版）

---

## 1. 原型现状亮点（保留）

- 信息架构清晰：底部四 Tab（囤货、使用、空瓶、设置）路径明确。
- 状态可视化强：安全/临期/即将过期胶囊 + 进度条 + 颜色表达一致。
- 关键动作突出：悬浮「+」贯穿全局，新增路径短。
- 产品详情采用 Bottom Sheet，符合移动端单手操作习惯。

---

## 2. 核心问题与优化方向

### 2.1 首页信息密度偏高（新手理解成本高）
**问题**
- 顶部有双层筛选（状态胶囊 + 分类胶囊）+ 卡片双进度条，首次使用认知负担较大。

**优化**
- 增加「简洁模式 / 专业模式」：
  - 简洁：默认只展示“实际到期剩余天数 + 建议动作”。
  - 专业：展示 PAO 与未开封双进度条。
- 将“按临期排序”做成显式可切换（临期优先 / 价格优先 / 最近开封）。

### 2.2 使用页是流水账，但缺少“行为引导”
**问题**
- 仅展示“已用 X 天”，但下一步行为（继续用/换季停用/提前开新）不明显。

**优化**
- 每条日志增加 CTA：
  - `继续使用`、`暂停`、`准备替换`
- 增加「今日建议」卡片：
  - 例如“今天优先使用：修丽可 CEF（还剩21天）”。

### 2.3 空瓶页偏展示，缺少增长价值
**问题**
- 目前更像成就墙，但没有复购洞察。

**优化**
- 空瓶卡片增加标签：`会回购 / 不回购 / 敏感不适`
- 自动生成复购清单：
  - 最近 90 天空瓶 + 评分≥4 的产品加入「复购建议」。

### 2.4 设置页付费价值有展示，但“解锁时机”偏后置
**问题**
- 付费提示主要在设置页，用户未形成价值感知前容易忽略。

**优化**
- 前置触发点（不打扰）：
  - 第 3 次新增产品后，提示“已帮你避免 X 元潜在浪费，解锁智能提醒”。
  - 首次出现临期产品时，展示 1 次智能提醒对比卡。

---

## 3. 关键交互优化（可直接改原型）

### 3.1 囤货页卡片结构改造（优先）
当前卡片字段较多，建议改为三行：
1) 品牌+品名 + 状态徽标
2) **实际到期日/剩余天数（主信息）**
3) 次信息折叠（PAO / 未开封）

> 规则：用户点开卡片后再看双时间轴，不在列表页强行展示全部。

### 3.2 新增弹窗分步化（减少填表放弃）
当前新增字段较全，建议拆成 2 步：
- Step1：品牌、品名、类型、是否已开封
- Step2：日期、PAO、图标/颜色（可选）

并增加：
- 一键套模板（护肤/彩妆常见 PAO）
- 扫码入口置顶（高级）

### 3.3 详情弹窗增加“立即处理”
在详情页底部增加并列按钮：
- `今天用一次`
- `标记空瓶`
- `延后提醒`

减少返回列表再操作的路径。

---

## 4. 数据与规则优化

### 4.1 优先级公式升级
当前仅考虑临期程度与价格，建议增加“最近使用时间”：

```text
priority_score = 到期紧急度*0.45 + 价格权重*0.2 + 已开封权重*0.2 + 最近使用衰减*0.15
```

### 4.2 告警分层升级（避免通知疲劳）
- P0：已过期（强提醒）
- P1：7天内（每日最多一次）
- P2：30天内（每周一次）
- P3：60天内（默认仅站内，不推送）

### 4.3 新增“季节维度”
部分产品季节性明显（夏季防晒、冬季面霜），建议增加季节标签：
- 夏季 / 冬季 / 全年
- 排序时按当季加权。

---

## 5. 商业化优化（更自然）

## 免费版
- 基础录入
- 50 件库存
- 基础提醒

## 高级版（建议 ¥18 买断起）
- 扫码录入
- 双效期智能提醒策略
- 无限库存
- 空瓶复购分析
- 数据导出（Excel/PDF）

## 触发式付费文案
- “本月已帮你规避潜在浪费 ¥xxx，解锁智能提醒继续省钱。”

---

## 6. 4 周迭代计划（基于现原型）

### Week 1（交互减负）
- 囤货列表卡片降噪
- 新增弹窗改 2 步
- 排序选项可见化

### Week 2（行为引导）
- 使用页 CTA
- 今日建议卡
- 详情页立即处理按钮

### Week 3（增长闭环）
- 空瓶复购标签
- 复购建议清单
- 触发式付费弹层

### Week 4（验证）
- A/B：简洁模式 vs 专业模式
- 指标复盘：7日留存、临期处理率、付费转化

---

## 7. 验收指标（建议）

- 新增成功率（打开新增弹窗 -> 提交）> 70%
- 首次临期处理完成率 > 55%
- 人均每周记录行为（新增/开封/空瓶/使用）> 4 次
- 付费转化率（出现价值提示后的 7 天）> 4%

---

## 8. 立即可执行的 5 个改动

1. 囤货列表默认仅显示“剩余天数 + 状态”
2. 新增改为 2 步并保留草稿
3. 使用页每条加“继续使用”按钮
4. 详情页增加“延后提醒”
5. 设置页权益卡改为“省钱结果”驱动

