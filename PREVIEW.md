# 本地预览方式

1. 在仓库根目录运行：
   ```bash
   python -m http.server 8080
   ```
2. 浏览器打开：
   - `http://localhost:8080/index.html`（推荐）
   - 或 `http://localhost:8080/mobile_preview.html`

> 已提供 `index.html`，便于静态托管平台直接预览。


3. 页面加载后会自动弹出“新增单品”弹窗，可直接试交互。
