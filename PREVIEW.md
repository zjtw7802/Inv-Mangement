# 本地/云端预览方式

## 一键启动（推荐）

在仓库根目录执行：

```bash
./start_preview.sh
```

脚本会：
- 以 `0.0.0.0:8080` 启动服务（适配云端开发环境）
- 输出可访问的预览地址（本地地址 + 若可识别则输出 Codespaces/Gitpod 地址）

## 手动启动

```bash
python -m http.server --bind 0.0.0.0 8080
```

## 访问路径

- `.../index.html`（推荐）
- `.../mobile_preview.html`

> 若你在网页云端 Git 开发环境，不能直接用你电脑的 `127.0.0.1`，请使用平台“Ports/端口”面板生成的外链地址。

3. 页面加载后会自动弹出“新增单品”弹窗，可直接试交互。
